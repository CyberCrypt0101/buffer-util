# buffer-util
Describe it as "A simple Python utility to process and store text data locally using a passkey
Run this command : pip install flet cryptography flet-desktop-light==0.82.2 --break-system-packages
After all set run this command too : python3 main.py
